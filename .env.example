# Safe local Docker Compose defaults. Do not use these secrets in production.
APP_PORT=8080
BUILD_FRONTEND=true
VITE_API_BASE_URL=/api/v1
POSTGRES_PASSWORD=signaldeck
AGENT_PLATFORM_ENCRYPTION_KEY=signaldeck-agent-platform-dev-key
