# Ledger Agent Platform UI Spec

> Status: Merged into `docs/ledger-agent-platform.md` as of 2026-05-08 (`12ced2d`).

The platform shell, page patterns, Workflow Package, Model Connection, Tools, Runs, and removed-route UI notes now live in `docs/ledger-agent-platform.md`.

Keep this file as a compatibility pointer for older links. Do not add new UI details here; update the canonical platform reference instead.
