# Ledger Agent Platform Functional Spec

> Status: Merged into `docs/ledger-agent-platform.md` as of 2026-05-08 (`12ced2d`).

The package-first platform route map, resource contracts, launch behavior, and acceptance baseline now live in `docs/ledger-agent-platform.md`.

Keep this file as a compatibility pointer for older links. Do not add new functional details here; update the canonical platform reference instead.
