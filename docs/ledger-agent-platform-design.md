# Ledger Agent Platform Technical Design

> Status: Merged into `docs/ledger-agent-platform.md` as of 2026-05-08 (`12ced2d`).

The package-first platform backend shape, runtime flow, security boundaries, frontend shape, deployment notes, and removed-route notes now live in `docs/ledger-agent-platform.md`.

Keep this file as a compatibility pointer for older links. Do not add new technical details here; update the canonical platform reference instead.
